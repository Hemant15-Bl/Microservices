spring:
  application:
    name: Hotel-Service
  config:
    import: optional:configserver:http://localhost:8085

  datasource:
    url: jdbc:postgresql://localhost:5432/Spring_micro
    username: postgres
    password: root    

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082
